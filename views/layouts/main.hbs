<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ site.title }}</title>
    {{> head }}
</head>
<body>

    <h1>{{ site.title }}</h1>

    {{{ body }}}

    {{> foot }}

</body>
</html>
